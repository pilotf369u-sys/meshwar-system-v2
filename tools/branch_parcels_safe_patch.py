from pathlib import Path
p=Path('branch-dashboard.html')
s=p.read_text(encoding='utf-8')

old="""function courierOptions(o){let h='<option value=\"\">بدون مندوب</option>';couriers.filter(c=>c.is_active!==false).forEach(c=>{const id=cloudId(c.id);h+=`<option value=\"${esc(id)}\" ${cloudId(o?.courier_id)===id?'selected':''}>${esc(c.name)}</option>`});return h}function focusSearch(clear=false){"""
new="""function courierOptions(o){let h='<option value=\"\">بدون مندوب</option>';couriers.filter(c=>c.is_active!==false).forEach(c=>{const id=cloudId(c.id);h+=`<option value=\"${esc(id)}\" ${cloudId(o?.courier_id)===id?'selected':''}>${esc(c.name)}</option>`});return h}function branchOrderQuantity(o){const d=parseDetails(o?.details),n=Number(d.quantity??o?.quantity??1);return Number.isFinite(n)&&n>0?Math.max(1,Math.floor(n)):1}function branchParcelsCount(o){const d=parseDetails(o?.details),n=Number(d.parcels_count??1);return Number.isFinite(n)&&n>=1?Math.max(1,Math.floor(n)):1}function branchOrderProductName(o){const d=parseDetails(o?.details);return String(d.product_name||o?.product_name||'منتج').trim()||'منتج'}function branchOrderCity(o){const d=parseDetails(o?.details),c=d.customer&&typeof d.customer==='object'?d.customer:{};return o?.governorate||o?.city||o?.state||d.governorate||d.state||d.city||c.governorate||c.state||c.city||'غير محددة'}function branchPaymentStatus(o){const d=parseDetails(o?.details),raw=String(d.payment_method||d.payment_status||o?.payment_status||o?.delivery_payment_type||'').trim(),v=raw.toLowerCase();if(/paid|prepaid|مدفوع|تم الدفع/.test(v)||raw==='paid_prepaid')return 'تم الدفع مسبقاً';if(/cod|cash|عند الاستلام|نقد/.test(v)||raw==='cod_full'||raw==='product_paid_delivery_cod')return 'الدفع عند الاستلام - COD';return raw||'الدفع عند الاستلام - COD'}function focusSearch(clear=false){"""
if old not in s: raise SystemExit('helper anchor not found')
s=s.replace(old,new,1)

old="""<td><b>${esc(o.order_code||o.id)}</b><br><span class=\"muted\">Sipariş: ${esc(o.reference_order_no||'---')}</span></td>"""
new="""<td><b>${esc(o.order_code||o.id)}</b><br><span class=\"muted\">Sipariş: ${esc(o.reference_order_no||'---')}</span><br><span class=\"muted\" style=\"font-weight:800;color:#334155\">القطع: ${branchOrderQuantity(o)} &nbsp;|&nbsp; 📦 الطرود: ${branchParcelsCount(o)}</span></td>"""
if old not in s: raise SystemExit('order summary cell not found')
s=s.replace(old,new,1)

old="""<td><button class=\"green\" onclick=\"saveOrder(decodeURIComponent('${enc}'))\">حفظ</button></td></tr>`"""
new="""<td><button class=\"green\" onclick=\"saveOrder(decodeURIComponent('${enc}'))\">حفظ</button> <button class=\"purple\" onclick=\"branchOpenOrderDetails(decodeURIComponent('${enc}'))\">تفاصيل</button> <button class=\"blue\" onclick=\"branchPrintShippingLabel(decodeURIComponent('${enc}'))\">طباعة ملصق</button></td></tr>`"""
if old not in s: raise SystemExit('order action cell not found')
s=s.replace(old,new,1)

old="""<div class=\"scan-item\"><b>الحالة / المندوب</b><br>${esc(order.status||'---')}<br>${esc(courier)}${notes?`<hr><b>ملاحظات:</b><br>${esc(notes)}`:''}</div>"""
new="""<div class=\"scan-item\"><b>الحالة / المندوب</b><br>${esc(order.status||'---')}<br>${esc(courier)}<br><b>عدد القطع:</b> ${branchOrderQuantity(order)} &nbsp; <b>عدد الطرود:</b> ${branchParcelsCount(order)}${notes?`<hr><b>ملاحظات:</b><br>${esc(notes)}`:''}</div>"""
if old not in s: raise SystemExit('scan card block not found')
s=s.replace(old,new,1)

old=""".select('id,order_code,reference_order_no,customer_id,customer_name,status,total_price,currency,delivery_fee,delivery_payment_type,courier_id,courier_name,settlement_id,settlement_status,parcel_image_url')"""
new=""".select('id,order_code,reference_order_no,customer_id,customer_name,status,total_price,currency,delivery_fee,delivery_payment_type,courier_id,courier_name,settlement_id,settlement_status,parcel_image_url,details,created_at,city,governorate')"""
if old not in s: raise SystemExit('account select not found')
s=s.replace(old,new,1)

old="""const rows=s.orders.map(o=>`<tr><td>${esc(o.order_code||o.id)}<br>${esc(o.reference_order_no||'')}</td><td>${esc(o.customer_name)}</td><td>${money(doorCollection(o),o.currency)}</td><td>${money(o.delivery_fee,o.currency)}</td><td>${money(netCustody(o),o.currency)}</td><td>${esc(o.status)}</td><td>${o.settlement_id?'معلق/مقفل':'غير مقفل'}</td></tr>`).join('');accountModalBody.innerHTML=`<p>التحصيل عند الباب: ${chips(s.gross)} | أجور التوصيل: ${chips(s.fees)} | صافي العهدة: ${chips(s.net)}</p><table><tr><th>الشحنة / Sipariş</th><th>العميل</th><th>التحصيل</th><th>الأجرة</th><th>الصافي</th><th>الحالة</th><th>التقفيل</th></tr>${rows||'<tr><td colspan=\"7\">لا توجد شحنات</td></tr>'}</table>`"""
new="""const rows=s.orders.map(o=>`<tr><td>${esc(o.order_code||o.id)}<br>${esc(o.reference_order_no||'')}</td><td>${esc(o.customer_name)}</td><td>${branchOrderQuantity(o)}</td><td>${branchParcelsCount(o)}</td><td>${money(doorCollection(o),o.currency)}</td><td>${money(o.delivery_fee,o.currency)}</td><td>${money(netCustody(o),o.currency)}</td><td>${esc(o.status)}</td><td>${o.settlement_id?'معلق/مقفل':'غير مقفل'}</td></tr>`).join('');accountModalBody.innerHTML=`<p>التحصيل عند الباب: ${chips(s.gross)} | أجور التوصيل: ${chips(s.fees)} | صافي العهدة: ${chips(s.net)}</p><table><tr><th>الشحنة / Sipariş</th><th>العميل</th><th>القطع</th><th>الطرود</th><th>التحصيل</th><th>الأجرة</th><th>الصافي</th><th>الحالة</th><th>التقفيل</th></tr>${rows||'<tr><td colspan=\"9\">لا توجد شحنات</td></tr>'}</table>`"""
if old not in s: raise SystemExit('account modal table not found')
s=s.replace(old,new,1)

old="""const rows=s.orders.map(o=>`<tr><td>${esc(o.order_code||o.id)}<br>${esc(o.reference_order_no||'')}</td><td>${esc(o.customer_name)}</td><td>${money(doorCollection(o),o.currency)}</td><td>${money(o.delivery_fee,o.currency)}</td><td>${money(netCustody(o),o.currency)}</td><td>${esc(o.status)}</td></tr>`).join('');openPrint('كشف حساب المندوب - '+esc(s.courier.name),`<p>الفرع: ${esc(currentBranch.name||currentBranch.branch_name||'')}</p><p>التحصيل عند الباب: ${chips(s.gross)} | أجور التوصيل: ${chips(s.fees)} | صافي العهدة: ${chips(s.net)}</p><table><tr><th>الشحنة / Sipariş</th><th>العميل</th><th>التحصيل</th><th>الأجرة</th><th>الصافي</th><th>الحالة</th></tr>${rows}</table>`)"""
new="""const rows=s.orders.map(o=>`<tr><td>${esc(o.order_code||o.id)}<br>${esc(o.reference_order_no||'')}</td><td>${esc(o.customer_name)}</td><td>${branchOrderQuantity(o)}</td><td>${branchParcelsCount(o)}</td><td>${money(doorCollection(o),o.currency)}</td><td>${money(o.delivery_fee,o.currency)}</td><td>${money(netCustody(o),o.currency)}</td><td>${esc(o.status)}</td></tr>`).join('');openPrint('كشف حساب المندوب - '+esc(s.courier.name),`<p>الفرع: ${esc(currentBranch.name||currentBranch.branch_name||'')}</p><p>التحصيل عند الباب: ${chips(s.gross)} | أجور التوصيل: ${chips(s.fees)} | صافي العهدة: ${chips(s.net)}</p><table><tr><th>الشحنة / Sipariş</th><th>العميل</th><th>القطع</th><th>الطرود</th><th>التحصيل</th><th>الأجرة</th><th>الصافي</th><th>الحالة</th></tr>${rows}</table>`)"""
if old not in s: raise SystemExit('courier statement not found')
s=s.replace(old,new,1)

anchor="""function openPrint(title,body){"""
insert="""function branchOpenOrderDetails(id){const o=orders.find(x=>cloudId(x.id)===cloudId(id));if(!o)return alert('تعذر العثور على الشحنة.');const d=parseDetails(o.details),qty=branchOrderQuantity(o),parcels=branchParcelsCount(o),city=branchOrderCity(o);let m=document.getElementById('branchOrderDetailsModal');if(!m){m=document.createElement('div');m.id='branchOrderDetailsModal';m.className='modal';m.innerHTML='<div class=\"modal-content\"><span class=\"close\" onclick=\"document.getElementById(\\'branchOrderDetailsModal\\').style.display=\\'none\\'\">&times;</span><h3>تفاصيل الشحنة</h3><div id=\"branchOrderDetailsBody\" class=\"account-grid\"></div></div>';document.body.appendChild(m)}document.getElementById('branchOrderDetailsBody').innerHTML=`<div class=\"account-card\"><b>رقم الطلب</b><div>${esc(o.order_code||o.id)}</div></div><div class=\"account-card\"><b>الحالة</b><div>${esc(o.status||'---')}</div></div><div class=\"account-card\"><b>عدد القطع</b><div>${qty}</div></div><div class=\"account-card\"><b>عدد الطرود</b><div>${parcels}</div></div><div class=\"account-card\"><b>المحافظة / المدينة</b><div>${esc(city)}</div></div><div class=\"account-card\"><b>طريقة الدفع</b><div>${esc(branchPaymentStatus(o))}</div></div><div class=\"account-card\"><b>المنتج</b><div>${esc(branchOrderProductName(o))}</div></div><div class=\"account-card\"><b>ملاحظات</b><div>${esc(d.notes||'---')}</div></div>`;m.style.display='block'}
function branchPrintShippingLabel(id){const o=orders.find(x=>cloudId(x.id)===cloudId(id));if(!o)return alert('تعذر العثور على الشحنة للطباعة.');const code=o.order_code||String(o.id).slice(0,8),product=branchOrderProductName(o),qty=branchOrderQuantity(o),parcels=branchParcelsCount(o),city=branchOrderCity(o),payment=branchPaymentStatus(o),date=new Date(o.created_at||Date.now()).toLocaleDateString('en-US',{year:'numeric',month:'2-digit',day:'2-digit'}),w=window.open('','_blank','width=420,height=360');if(!w)return alert('يرجى السماح بالنوافذ المنبثقة لطباعة الملصق.');w.document.write(`<!doctype html><html lang=\"ar\" dir=\"rtl\"><head><meta charset=\"utf-8\"><title>MeshWar Cargo - ${esc(code)}</title><style>*{box-sizing:border-box}html,body{width:50mm;height:40mm;margin:0;padding:1.5mm;font-family:Arial,sans-serif;color:#000;background:#fff;font-size:8px;overflow:hidden}.label-card{width:100%;height:100%;border:1px solid #000;padding:2px;display:flex;flex-direction:column;justify-content:space-between;gap:1px;overflow:hidden}.brand{text-align:center;border-bottom:1px solid #000;padding-bottom:1px;line-height:1}.brand h1{margin:0;font-size:9px;font-weight:900}.brand p{margin:0;font-size:6.5px}.order{text-align:center;font-size:9px;font-weight:900;line-height:1;direction:ltr}.barcode{height:22px;min-height:22px;display:flex;align-items:center;justify-content:center;overflow:hidden}.barcode svg{width:100%;height:22px}.grid{display:grid;grid-template-columns:1fr 1fr;border:1px solid #000;line-height:1.05}.cell{padding:1px;border:1px solid #cbd5e1;min-width:0}.label-title{font-size:6.5px;color:#475569}.value{font-size:7.5px;font-weight:800;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.product{grid-column:1/-1}@media print{@page{size:50mm 40mm;margin:0}html,body{width:50mm!important;height:40mm!important;margin:0!important;padding:1.5mm!important;overflow:hidden!important}.label-card{width:100%;height:100%;max-height:37mm;page-break-inside:avoid!important;page-break-after:avoid!important;overflow:hidden!important}}</style><script src=\"https://cdn.jsdelivr.net/npm/jsbarcode@3.11.6/dist/JsBarcode.all.min.js\"><\\/script></head><body><div class=\"label-card\"><div class=\"brand\"><h1>MeshWar Cargo</h1><p>ملصق شحنة الفرع</p></div><div class=\"order\">${esc(code)}</div><div class=\"barcode\"><svg id=\"barcode\"></svg></div><div class=\"grid\"><div class=\"cell product\"><div class=\"label-title\">المنتج</div><div class=\"value\">${esc(product)}</div></div><div class=\"cell\"><div class=\"label-title\">عدد القطع</div><div class=\"value\">${qty}</div></div><div class=\"cell\"><div class=\"label-title\">عدد الطرود</div><div class=\"value\">${parcels}</div></div><div class=\"cell\"><div class=\"label-title\">التاريخ</div><div class=\"value\">${esc(date)}</div></div><div class=\"cell\"><div class=\"label-title\">المحافظة / المدينة</div><div class=\"value\">${esc(city)}</div></div><div class=\"cell\"><div class=\"label-title\">حالة الدفع</div><div class=\"value\">${esc(payment)}</div></div></div></div><script>window.addEventListener('load',()=>{try{JsBarcode('#barcode',${JSON.stringify(String(code))},{format:'CODE128',displayValue:false,height:22,margin:0,width:1.25})}catch(e){console.error(e)}setTimeout(()=>window.print(),250)});<\\/script></body></html>`);w.document.close()}
"""+anchor
if anchor not in s: raise SystemExit('openPrint anchor not found')
s=s.replace(anchor,insert,1)

p.write_text(s,encoding='utf-8')
print('branch patch applied')
