from pathlib import Path

def req(s,a,b,label):
    if a not in s: raise SystemExit('Missing '+label)
    return s.replace(a,b,1)

p=Path('employee-dashboard.html');s=p.read_text(encoding='utf-8')
s=req(s,"branch:{label:'🏢 محولة إلى الفرع',branch:true,statuses:['تجهيز شحن','مخزن محلي','مندوب','توزيع داخلي']},in_order:","branch:{label:'🏢 محولة إلى الفرع',branch:true,statuses:['تجهيز شحن','مخزن محلي','توزيع داخلي']},delivery:{label:'🛵 مندوب',statuses:['مندوب']},in_order:",'employee pipeline')
s=s.replace("let employeeSupabase=null,currentEmployee=null,employeePermissions={},cloudOrders=[],cloudBranches=[],cloudCustomers=[];","let employeeSupabase=null,currentEmployee=null,employeePermissions={},cloudOrders=[],cloudBranches=[],cloudCustomers=[],cloudCouriers=[];")
s=req(s,"async function loadBranches(){","async function loadCouriersForOrders(){const sb=await ensureEmployeeSupabase(),{data,error}=await sb.from('couriers').select('*').order('name',{ascending:true}).limit(2000);cloudCouriers=error?[]:(data||[])}function courierOptionsForOrder(o){const cur=cloudId(o.delivery_agent_id||o.courier_id);return '<option value=\"\">اختر موظف التوصيل</option>'+cloudCouriers.filter(c=>!o.branch_id||cloudId(c.branch_id)===cloudId(o.branch_id)).map(c=>`<option value=\"${escapeHtml(cloudId(c.id))}\" ${cloudId(c.id)===cur?'selected':''}>${escapeHtml(c.name||c.phone||c.id)}</option>`).join('')}\nasync function loadBranches(){",'employee helpers')
s=s.replace("await loadEmployeeIdentity();await loadBranches();","await loadEmployeeIdentity();await Promise.all([loadBranches(),loadCouriersForOrders()]);")
s=req(s,"<td><select id=\"status-${id}\">${statusOptions(status)}</select></td><td>","<td><select id=\"status-${id}\" onchange=\"document.getElementById('courier-${id}').style.display=this.value==='مندوب'?'block':'none'\">${statusOptions(status)}</select><select id=\"courier-${id}\" style=\"margin-top:6px;display:${status==='مندوب'?'block':'none'}\">${courierOptionsForOrder(o)}</select></td><td>",'employee selector')
a="const reference=document.getElementById('ref-'+encodedId)?.value?.trim()||null,payment=paymentType(document.getElementById('pay-'+encodedId)?.value),branchId=cloudId(document.getElementById('branch-'+encodedId)?.value),branch=cloudBranches.find(b=>cloudId(b.id)===branchId),payload={status,reference_order_no:reference,delivery_payment_type:payment,branch_id:branchId||null,branch_name:branch?(branch.name||branch.branch_name||null):null};"
b="const reference=document.getElementById('ref-'+encodedId)?.value?.trim()||null,payment=paymentType(document.getElementById('pay-'+encodedId)?.value),branchId=cloudId(document.getElementById('branch-'+encodedId)?.value),branch=cloudBranches.find(b=>cloudId(b.id)===branchId),agentId=cloudId(document.getElementById('courier-'+encodedId)?.value),payload={status,reference_order_no:reference,delivery_payment_type:payment,branch_id:branchId||null,branch_name:branch?(branch.name||branch.branch_name||null):null};if(status==='مندوب'){if(!agentId)return alert('اختر موظف التوصيل أولاً');payload.delivery_agent_id=agentId;payload.courier_id=agentId}else if(o.status==='مندوب'){payload.delivery_agent_id=null;payload.courier_id=null}"
s=req(s,a,b,'employee payload');p.write_text(s,encoding='utf-8')

p=Path('admin-dashboard.html');s=p.read_text(encoding='utf-8')
s=req(s,"branch:{label:'🏢 محولة إلى الفرع',branch:true,statuses:['تجهيز شحن','مخزن محلي','مندوب','توزيع داخلي']},in_order:","branch:{label:'🏢 محولة إلى الفرع',branch:true,statuses:['تجهيز شحن','مخزن محلي','توزيع داخلي']},delivery:{label:'🛵 مندوب',statuses:['مندوب']},in_order:",'admin pipeline')
s=s.replace("adminBranches=[],adminRewardCustomers=[];","adminBranches=[],adminRewardCustomers=[],adminCouriersForOrders=[];")
s=req(s,"async function loadAdminBranches(){","async function loadAdminCouriersForOrders(){const sb=await ensureCustomerSupabase(),{data,error}=await sb.from('couriers').select('*').order('name',{ascending:true}).limit(2000);adminCouriersForOrders=error?[]:(data||[])}function adminCourierOptions(o){const cur=String(o.delivery_agent_id||o.courier_id||'');return '<option value=\"\">اختر موظف التوصيل</option>'+adminCouriersForOrders.filter(c=>!o.branch_id||String(c.branch_id)===String(o.branch_id)).map(c=>`<option value=\"${esc(c.id)}\" ${String(c.id)===cur?'selected':''}>${esc(c.name||c.phone||c.id)}</option>`).join('')}\nasync function loadAdminBranches(){",'admin helpers')
s=s.replace("await loadAdminBranches();renderAdminPipelineTabs();","await Promise.all([loadAdminBranches(),loadAdminCouriersForOrders()]);renderAdminPipelineTabs();")
s=req(s,"<td><select id=\"adminStatus-${id}\">${statusOptions(status)}</select><div id=\"adminRejectWrap-${id}\"","<td><select id=\"adminStatus-${id}\" onchange=\"document.getElementById('adminCourier-${id}').style.display=this.value==='مندوب'?'block':'none'\">${statusOptions(status)}</select><select id=\"adminCourier-${id}\" style=\"margin-top:6px;display:${status==='مندوب'?'block':'none'}\">${adminCourierOptions(o)}</select><div id=\"adminRejectWrap-${id}\"",'admin selector')
a="raw=document.getElementById('adminPrice-'+encodedId)?.value??'',payload={reference_order_no:document.getElementById('adminRef-'+encodedId)?.value.trim()||null,delivery_payment_type:payType(document.getElementById('adminPay-'+encodedId)?.value),branch_id:branchId||null,branch_name:branch?(branch.name||branch.branch_name||null):null,status,rejection_reason:status==='رفض الطلب'?reason:null};"
b="raw=document.getElementById('adminPrice-'+encodedId)?.value??'',agentId=String(document.getElementById('adminCourier-'+encodedId)?.value||'').trim(),payload={reference_order_no:document.getElementById('adminRef-'+encodedId)?.value.trim()||null,delivery_payment_type:payType(document.getElementById('adminPay-'+encodedId)?.value),branch_id:branchId||null,branch_name:branch?(branch.name||branch.branch_name||null):null,status,rejection_reason:status==='رفض الطلب'?reason:null};if(status==='مندوب'){if(!agentId)return alert('اختر موظف التوصيل أولاً');payload.delivery_agent_id=agentId;payload.courier_id=agentId}else if(o.status==='مندوب'){payload.delivery_agent_id=null;payload.courier_id=null}"
s=req(s,a,b,'admin payload');p.write_text(s,encoding='utf-8')

p=Path('delivery-dashboard.html');s=p.read_text(encoding='utf-8')
s=s.replace(".tabs{display:grid;grid-template-columns:repeat(4,1fr);gap:8px;margin-bottom:12px}",".delivery-layout{display:grid;grid-template-columns:220px minmax(0,1fr);gap:14px;align-items:start}.tabs{display:flex;flex-direction:column;gap:8px;position:sticky;top:12px;background:#fff;border:1px solid var(--line);border-radius:14px;padding:10px}.delivery-main{min-width:0}")
s=s.replace(".orders{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}",".orders{display:flex;flex-direction:column;gap:9px}")
s=s.replace(".shipment{background:#fff;border:1px solid var(--line);border-radius:15px;padding:14px;box-shadow:0 3px 12px #0f172a0c}",".shipment{background:#fff;border:1px solid var(--line);border-radius:13px;box-shadow:0 3px 12px #0f172a0c;overflow:hidden}.shipment-summary{padding:13px;cursor:pointer;display:grid;grid-template-columns:1.1fr 1.2fr 2fr 1fr auto;gap:10px;align-items:center}.shipment-details{display:none;padding:0 14px 14px;border-top:1px solid #eef2f7}.shipment.open .shipment-details{display:block}.chevron{font-size:18px;color:#64748b}.shipment.open .chevron{transform:rotate(180deg)}")
s=s.replace("@media(max-width:850px){.finance{grid-template-columns:1fr 1fr}.orders{grid-template-columns:1fr}.tabs{grid-template-columns:1fr 1fr}}","@media(max-width:850px){.finance{grid-template-columns:1fr 1fr}.delivery-layout{grid-template-columns:155px minmax(0,1fr)}.shipment-summary{grid-template-columns:1fr 1fr}.summary-address{grid-column:1/-1}}")
s=s.replace("@media(max-width:520px){.app{padding:8px}","@media(max-width:620px){.delivery-layout{grid-template-columns:112px minmax(0,1fr)}.tabs{padding:6px}.tab{font-size:11px;padding:9px 5px}.shipment-summary{grid-template-columns:1fr}.summary-address{grid-column:auto}}@media(max-width:520px){.app{padding:8px}")
s=req(s,"<div class=\"tabs\">","<div class=\"delivery-layout\"><aside class=\"tabs\">",'sidebar open')
s=req(s,"</button></div>\n<div class=\"toolbar\">","</button></aside><main class=\"delivery-main\">\n<div class=\"toolbar\">",'sidebar main')
s=req(s,"</span><button id=\"nextBtn\" onclick=\"changeDeliveryPage(1)\">التالي</button></div>\n</div>\n<script>","</span><button id=\"nextBtn\" onclick=\"changeDeliveryPage(1)\">التالي</button></div></main></div>\n</div>\n<script>",'layout close')
s=s.replace("function money(v,c){return Number(v||0).toFixed(2)+' '+esc(c||'$')}","function money(v,c){return Number(v||0).toFixed(2)+(c?' '+esc(c):'')}")
s=s.replace("select('id,code,name,phone,secondary_phone,address,state,governorate')","select('*')")
s=s.replace("const cur=o.currency||'$'","const cur=o.currency||'بدون عملة'")
s=s.replace("cur=o.currency||'$';return","cur=o.currency||'';return")
s=s.replace(".eq('courier_id',idText(currentCourier.id)).eq('branch_id'",".or(`delivery_agent_id.eq.${idText(currentCourier.id)},courier_id.eq.${idText(currentCourier.id)}`).eq('branch_id'")
s=s.replace("let q=sb.from('orders').select('*').eq('courier_id',idText(currentCourier.id)).eq('branch_id'","let q=sb.from('orders').select('*').or(`delivery_agent_id.eq.${idText(currentCourier.id)},courier_id.eq.${idText(currentCourier.id)}`).eq('branch_id'")
s=s.replace(".eq('id',id).eq('courier_id',idText(currentCourier.id))",".eq('id',id).or(`delivery_agent_id.eq.${idText(currentCourier.id)},courier_id.eq.${idText(currentCourier.id)}`)")
s=s.replace("if(idText(row.courier_id)===idText(currentCourier.id))","if(idText(row.delivery_agent_id||row.courier_id)===idText(currentCourier.id))")
start=s.index('function renderOrderCard(o){');end=s.index('\nfunction toggleDeliveryFields',start)
renderer='''function customerFullAddress(o,c){const d=parseDetails(o.details),parts=[c.governorate||c.state||c.province||'',c.city||'',c.district||'',c.address||'',o.address||o.recipient_address||o.delivery_address||d.address||d.recipient_address||d.delivery_address||''];return [...new Set(parts.map(x=>String(x||'').trim()).filter(Boolean))].join(' — ')||'---'}
function toggleShipmentCard(id){const raw=decodeURIComponent(id),el=document.querySelector(`.shipment[data-id="${CSS.escape(raw)}"]`);if(el)el.classList.toggle('open')}
function renderOrderCard(o){const id=encodeURIComponent(idText(o.id)),c=o._customer||{},phone=o.customer_phone||c.phone||'',second=c.secondary_phone||c.phone2||c.alternate_phone||'',digits=phoneDigits(phone),message=encodeURIComponent(`مرحباً ${o.customer_name||c.name||''}، معك مندوب MeshWar بخصوص الطلب ${o.order_code||o.id}. أنا في الطريق إليك للتوصيل.`),d=parseDetails(o.details),deferDate=d.delivery_deferred_until||'',note=d.delivery_note||'',reason=d.delivery_rejection_reason||o.rejection_reason||'',cur=o.currency||'',address=customerFullAddress(o,c),amount=doorCollection(o);return `<article class="shipment" data-id="${esc(o.id)}"><div class="shipment-summary" onclick="toggleShipmentCard('${id}')"><div><div class="code">${esc(o.order_code||o.id)}</div><div class="muted">${esc(o.reference_order_no||'')}</div></div><div><b>${esc(o.customer_name||c.name||'---')}</b></div><div class="summary-address">${esc(address)}</div><div class="money">${money(amount,cur)}</div><span class="chevron">⌄</span></div><div class="shipment-details"><div class="shipment-head" style="margin-top:12px"><div class="muted">كود العميل: ${esc(c.code||o.customer_id||'---')}</div><span class="status">${esc(o.status||'')}</span></div><div class="grid"><div class="item"><b>الهاتف الرئيسي</b><span dir="ltr">${esc(phone||'---')}</span>${second?`<div class="secondary">الهاتف الاحتياطي: <span dir="ltr">${esc(second)}</span></div>`:''}</div><div class="item"><b>العنوان الكامل</b>${esc(address)}</div><div class="item"><b>التحصيل عند الباب</b><span class="money">${money(amount,cur)}</span><br><span class="muted">أجرة التوصيل: ${money(o.delivery_fee,cur)}</span></div><div class="item"><b>صافي العهدة</b><span class="money">${money(deliveryNet(o),cur)}</span>${o.is_settled?'<div class="secondary" style="color:#15803d">مصفّى ماليًا</div>':''}</div></div><div class="actions">${digits?`<a class="btn btn-whatsapp" target="_blank" rel="noopener" href="https://wa.me/${digits}?text=${message}">واتساب</a><a class="btn btn-call" href="tel:${esc(phone)}">اتصال</a>`:''}${second?`<a class="btn btn-call" href="tel:${esc(second)}">اتصال احتياطي</a>`:''}</div><div class="edit-box"><select id="st-${id}" onchange="toggleDeliveryFields('${id}')">${statusOptions(deliveryTab,o.status,d)}</select><input id="defer-${id}" type="date" value="${esc(deferDate)}" style="display:${(d.delivery_state==='deferred'||d.delivery_deferred_until)?'block':'none'}"><textarea id="note-${id}" placeholder="ملاحظة التأجيل / التوصيل">${esc(note)}</textarea><select id="rating-${id}" class="full"><option value="">تقييم تجربة التسليم</option>${[1,2,3,4,5].map(n=>`<option value="${n}" ${Number(d.delivery_rating||0)===n?'selected':''}>${'⭐'.repeat(n)} (${n}/5)</option>`).join('')}</select><input class="full" id="reason-${id}" placeholder="سبب الرفض / الراجع" value="${esc(reason)}" style="display:${DELIVERY_TABS.rejected.includes(o.status)?'block':'none'}"><button class="btn btn-green full" onclick="saveDeliveryOrder('${id}')">حفظ تحديث الشحنة</button></div></div></article>`}'''
s=s[:start]+renderer+s[end:]
p.write_text(s,encoding='utf-8')
