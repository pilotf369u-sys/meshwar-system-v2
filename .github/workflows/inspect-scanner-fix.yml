name: Inspect scanner fix
on:
  push:
    paths:
      - '.github/inspect_scanner_fix.py'
permissions:
  contents: read
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: python3 .github/inspect_scanner_fix.py
