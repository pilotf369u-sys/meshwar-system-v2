name: Global delivery search patch
on:
  push:
    paths:
      - '.github/global_delivery_search_patch.py'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - run: python3 .github/global_delivery_search_patch.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add delivery-dashboard.html
          git commit -m "Search delivery orders across all states" || exit 0
          git push origin main
