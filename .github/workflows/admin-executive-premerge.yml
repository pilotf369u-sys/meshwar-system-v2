name: Admin premerge smoke
on:
  push:
    branches:
      - agent/admin-executive-gold
permissions:
  contents: write
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/admin-executive-gold
      - name: Smoke
        run: echo admin-premerge-ok
