/* KINTO V98 — secure vendor shipping settings UI. */
(()=>{'use strict';
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const uid=()=>crypto.randomUUID?.()||`tmp-${Date.now()}-${Math.random().toString(36).slice(2)}`;
let state={version:0,is_active:false,default_currency:'IQD',companies:[],rates:[]},loading=false,saving=false,nativeSetTab=null;
function api(){return window.MeshwarVendorV94}
function runtime(){return window.MeshwarVendorRuntime}
function session(){return api()?.session?.()||null}
function notice(message,bad=false){runtime()?.showNotice?.(message,bad)}
function money(v,c){return runtime()?.money?.(v,c)||`${Number(v||0).toLocaleString('en-US')} ${c||''}`}
function normalize(data){return{version:Number(data?.version)||0,is_active:data?.is_active===true,default_currency:String(data?.default_currency||'IQD').toUpperCase(),companies:Array.isArray(data?.companies)?data.companies.map(x=>({...x,key:String(x.key||x.id||uid())})):[],rates:Array.isArray(data?.rates)?data.rates.map(x=>({...x,id:String(x.id||uid()),company_key:String(x.company_key||''),destination_country:x.destination_country||'*',governorate:x.governorate||'*',area:x.area||'*',currency:String(x.currency||data?.default_currency||'IQD').toUpperCase()})):[]}}
function currencyOptions(value){return['IQD','USD','TRY'].map(x=>`<option value="${x}" ${x===value?'selected':''}>${x}</option>`).join('')}
function companyOptions(value){return state.companies.map(c=>`<option value="${esc(c.key)}" ${String(c.key)===String(value)?'selected':''}>${esc(c.company_name||'شركة بدون اسم')}</option>`).join('')}
function ensureUi(){
  const nav=document.querySelector('.vendor-main-tabs');if(!nav||$('vendorTab-shipping'))return false;
  if(!$('vendorShippingV98Style')){const style=document.createElement('style');style.id='vendorShippingV98Style';style.textContent='.vendor-main-tabs{grid-template-columns:repeat(4,minmax(0,1fr))}@media(max-width:900px){.vendor-main-tabs{grid-template-columns:1fr}}';document.head.appendChild(style)}
  const button=document.createElement('button');button.id='vendorTabBtn-shipping';button.type='button';button.className='vendor-main-tab';button.textContent='🚚 الشحن والتوصيل';button.onclick=()=>setTab('shipping');nav.appendChild(button);
  const panel=document.createElement('section');panel.id='vendorTab-shipping';panel.className='vendor-tab-panel';panel.innerHTML=`<div class="glass rounded-3xl p-4 shadow-glass md:p-6"><div class="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h2 class="vendor-text text-xl font-black">إعدادات الشحن والتوصيل</h2><p class="vendor-muted mt-1 text-xs text-slate-400">تُثبت التعرفة المختارة داخل الطلب لحظة إنشائه ولا تتغير لاحقاً.</p></div><div class="flex gap-2"><button id="vendorShippingReload" type="button" class="rounded-xl border border-white/10 bg-white/5 px-4 py-2 text-xs font-black">↻ تحديث</button><button id="vendorShippingSave" type="button" class="rounded-xl bg-emerald-600 px-4 py-2 text-xs font-black text-white">حفظ الإعدادات</button></div></div><div class="mb-5 grid gap-3 rounded-2xl border border-white/10 bg-white/5 p-4 md:grid-cols-2"><label class="flex items-center gap-3 font-black"><input id="vendorShippingActive" type="checkbox" class="h-5 w-5 accent-emerald-500"> تفعيل تسعير الشحن تلقائياً للطلبات الجديدة</label><label><span class="vendor-muted mb-1 block text-xs">العملة الافتراضية</span><select id="vendorShippingCurrency" class="field">${currencyOptions('IQD')}</select></label><p class="vendor-muted text-xs md:col-span-2">عند التفعيل، تُرفض وجهة العميل غير المغطاة بدلاً من إنشاء طلب بتكلفة خاطئة. يجب أن تطابق عملة التعرفة عملة الطلب.</p></div><div class="mb-6"><div class="mb-3 flex items-center justify-between"><h3 class="vendor-text font-black">شركات الشحن الخاصة بك</h3><button id="vendorShippingAddCompany" type="button" class="rounded-xl bg-sky-600 px-3 py-2 text-xs font-black">+ شركة شحن</button></div><div id="vendorShippingCompanies" class="grid gap-3"></div></div><div><div class="mb-3 flex items-center justify-between"><h3 class="vendor-text font-black">التعرفة حسب المحافظة / المنطقة</h3><button id="vendorShippingAddRate" type="button" class="rounded-xl bg-indigo-600 px-3 py-2 text-xs font-black">+ تعرفة</button></div><div id="vendorShippingRates" class="grid gap-3"></div></div><div id="vendorShippingSummary" class="vendor-muted mt-5 rounded-xl border border-white/10 bg-white/5 p-3 text-xs"></div></div>`;
  const main=document.querySelector('#dashboardView main');main?.appendChild(panel);
  $('vendorShippingReload').onclick=load;
  $('vendorShippingSave').onclick=save;
  $('vendorShippingAddCompany').onclick=()=>{state.companies.push({key:uid(),company_name:'',phone:'',tracking_url:'',is_active:true,sort_order:state.companies.length});render()};
  $('vendorShippingAddRate').onclick=()=>{if(!state.companies.length)return notice('أضف شركة شحن أولاً.',true);state.rates.push({id:uid(),company_key:state.companies[0].key,destination_country:'*',governorate:'*',area:'*',delivery_fee:0,currency:state.default_currency,estimated_days_min:null,estimated_days_max:null,priority:0,is_active:true});render()};
  $('vendorShippingActive').onchange=e=>{state.is_active=e.target.checked;renderSummary()};
  $('vendorShippingCurrency').onchange=e=>{state.default_currency=e.target.value;renderSummary()};
  return true;
}
function setTab(tab){
  if(tab!=='shipping'){ $('vendorTab-shipping')?.classList.remove('active');$('vendorTabBtn-shipping')?.classList.remove('active');return nativeSetTab?.(tab)}
  ['orders','finance','products'].forEach(name=>{document.getElementById('vendorTab-'+name)?.classList.remove('active');document.getElementById('vendorTabBtn-'+name)?.classList.remove('active')});
  $('vendorTab-shipping')?.classList.add('active');$('vendorTabBtn-shipping')?.classList.add('active');void load();
}
function readCompany(key){const root=document.querySelector(`[data-shipping-company="${CSS.escape(key)}"]`);if(!root)return null;return{key,company_name:root.querySelector('[data-field="name"]')?.value.trim()||'',phone:root.querySelector('[data-field="phone"]')?.value.trim()||'',tracking_url:root.querySelector('[data-field="url"]')?.value.trim()||'',is_active:root.querySelector('[data-field="active"]')?.checked!==false,sort_order:Number(root.querySelector('[data-field="sort"]')?.value)||0}}
function readRate(id){const root=document.querySelector(`[data-shipping-rate="${CSS.escape(id)}"]`);if(!root)return null;const value=f=>root.querySelector(`[data-field="${f}"]`)?.value;return{id,company_key:value('company'),destination_country:value('country')?.trim()||'*',governorate:value('governorate')?.trim()||'*',area:value('area')?.trim()||'*',delivery_fee:Number(value('fee')),currency:value('currency')||state.default_currency,estimated_days_min:value('min')===''?null:Number(value('min')),estimated_days_max:value('max')===''?null:Number(value('max')),priority:Number(value('priority'))||0,is_active:root.querySelector('[data-field="active"]')?.checked!==false}}
function syncDom(){state.companies=state.companies.map(c=>readCompany(c.key)||c);state.rates=state.rates.map(r=>readRate(r.id)||r);state.is_active=$('vendorShippingActive')?.checked===true;state.default_currency=$('vendorShippingCurrency')?.value||state.default_currency}
function render(){
  if(!$('vendorShippingCompanies'))return;
  $('vendorShippingActive').checked=state.is_active;$('vendorShippingCurrency').innerHTML=currencyOptions(state.default_currency);
  $('vendorShippingCompanies').innerHTML=state.companies.map(c=>`<article data-shipping-company="${esc(c.key)}" class="grid gap-2 rounded-2xl border border-white/10 bg-white/5 p-3 md:grid-cols-6"><input data-field="name" class="field md:col-span-2" value="${esc(c.company_name||'')}" placeholder="اسم شركة الشحن"><input data-field="phone" class="field" value="${esc(c.phone||'')}" placeholder="الهاتف"><input data-field="url" class="field md:col-span-2" value="${esc(c.tracking_url||'')}" placeholder="https://tracking..."><div class="flex items-center gap-2"><input data-field="sort" class="field w-20" type="number" min="0" value="${Number(c.sort_order)||0}" title="الترتيب"><label class="text-xs"><input data-field="active" type="checkbox" ${c.is_active!==false?'checked':''}> فعالة</label><button type="button" data-remove-company="${esc(c.key)}" class="rounded-lg bg-rose-600 px-2 py-1 text-xs">حذف</button></div></article>`).join('')||'<div class="vendor-muted rounded-xl border border-dashed border-white/10 p-5 text-center text-sm">لا توجد شركات شحن بعد.</div>';
  $('vendorShippingRates').innerHTML=state.rates.map(r=>`<article data-shipping-rate="${esc(r.id)}" class="grid gap-2 rounded-2xl border border-white/10 bg-white/5 p-3 md:grid-cols-4"><label><span class="vendor-muted mb-1 block text-xs">شركة الشحن</span><select data-field="company" class="field">${companyOptions(r.company_key)}</select></label><label><span class="vendor-muted mb-1 block text-xs">الدولة (* للكل)</span><input data-field="country" class="field" value="${esc(r.destination_country||'*')}"></label><label><span class="vendor-muted mb-1 block text-xs">المحافظة (* للكل)</span><input data-field="governorate" class="field" value="${esc(r.governorate||'*')}"></label><label><span class="vendor-muted mb-1 block text-xs">المنطقة (* للكل)</span><input data-field="area" class="field" value="${esc(r.area||'*')}"></label><label><span class="vendor-muted mb-1 block text-xs">تكلفة التوصيل</span><input data-field="fee" class="field" type="number" min="0" step="0.01" value="${Number(r.delivery_fee)||0}"></label><label><span class="vendor-muted mb-1 block text-xs">العملة</span><select data-field="currency" class="field">${currencyOptions(r.currency||state.default_currency)}</select></label><label><span class="vendor-muted mb-1 block text-xs">مدة التوصيل</span><span class="flex gap-2"><input data-field="min" class="field" type="number" min="0" value="${r.estimated_days_min??''}" placeholder="من"><input data-field="max" class="field" type="number" min="0" value="${r.estimated_days_max??''}" placeholder="إلى"></span></label><div><span class="vendor-muted mb-1 block text-xs">الأولوية</span><div class="flex items-center gap-2"><input data-field="priority" class="field w-24" type="number" value="${Number(r.priority)||0}"><label class="text-xs"><input data-field="active" type="checkbox" ${r.is_active!==false?'checked':''}> فعالة</label><button type="button" data-remove-rate="${esc(r.id)}" class="rounded-lg bg-rose-600 px-2 py-1 text-xs">حذف</button></div></div></article>`).join('')||'<div class="vendor-muted rounded-xl border border-dashed border-white/10 p-5 text-center text-sm">لا توجد تعريفات توصيل بعد.</div>';
  document.querySelectorAll('[data-remove-company]').forEach(b=>b.onclick=()=>{syncDom();const key=b.dataset.removeCompany;state.companies=state.companies.filter(c=>c.key!==key);state.rates=state.rates.filter(r=>r.company_key!==key);render()});
  document.querySelectorAll('[data-remove-rate]').forEach(b=>b.onclick=()=>{syncDom();state.rates=state.rates.filter(r=>r.id!==b.dataset.removeRate);render()});renderSummary();
}
function renderSummary(){const box=$('vendorShippingSummary');if(!box)return;const active=state.rates.filter(r=>r.is_active!==false);box.textContent=`الحالة: ${state.is_active?'مفعّل':'غير مفعّل'} • الشركات: ${state.companies.length} • التعرفات: ${active.length} • إصدار الإعدادات: ${state.version}`}
async function load(){if(loading)return;const active=session();if(!active?.token)return notice('سجّل الدخول مجدداً لإدارة إعدادات الشحن الآمنة.',true);loading=true;try{const{data,error}=await runtime().sb.rpc('vendor_get_shipping_settings',{p_session_token:active.token});if(error)throw error;state=normalize(data);render()}catch(e){console.error('Vendor shipping load failed',e);notice('تعذر تحميل إعدادات الشحن: '+(e.message||e),true)}finally{loading=false}}
async function save(){if(saving)return;const active=session();if(!active?.token)return notice('سجّل الدخول مجدداً لحفظ إعدادات الشحن.',true);syncDom();if(state.companies.some(c=>!c.company_name))return notice('اسم شركة الشحن مطلوب.',true);if(state.rates.some(r=>!r.company_key||!r.governorate||!Number.isFinite(r.delivery_fee)||r.delivery_fee<0))return notice('تحقق من الشركة والمحافظة والتكلفة في جميع التعرفات.',true);saving=true;const button=$('vendorShippingSave');if(button){button.disabled=true;button.textContent='جاري الحفظ...'}try{const payload={p_session_token:active.token,p_expected_version:state.version,p_is_active:state.is_active,p_default_currency:state.default_currency,p_companies:state.companies,p_rates:state.rates},{data,error}=await runtime().sb.rpc('vendor_save_shipping_settings',payload);if(error)throw error;state=normalize(data);render();notice('تم حفظ إعدادات الشحن. ستطبق على الطلبات الجديدة فقط.')}catch(e){console.error('Vendor shipping save failed',e);if(String(e.message||e).includes('VERSION_CONFLICT')){notice('تغيرت الإعدادات في جلسة أخرى؛ أعدنا تحميل أحدث نسخة.',true);await load()}else notice('تعذر حفظ إعدادات الشحن: '+(e.message||e),true)}finally{saving=false;if(button){button.disabled=false;button.textContent='حفظ الإعدادات'}}}
function install(){if(!ensureUi())return false;if(!nativeSetTab){nativeSetTab=window.setVendorTab?.bind(window);window.setVendorTab=setTab}return true}
let attempts=0;const timer=setInterval(()=>{attempts++;if(runtime()&&api()&&install()){clearInterval(timer);return}if(attempts>160)clearInterval(timer)},100);
window.MeshwarVendorShippingV98={install,load,save,getState:()=>JSON.parse(JSON.stringify(state)),version:'v98'};
})();
