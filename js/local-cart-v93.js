/* KINTO V94 — global multi-store cart + single global order checkout. */
(()=>{'use strict';
const PREFIX='kinto_global_cart_v94',SUPABASE_URL='https://hsmmbloouskqdnptiiad.supabase.co',SUPABASE_KEY='sb_publishable_6_IDhNRdtxboDuCfBeAulQ_RRrBqpFH';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const money=v=>Number(v||0).toLocaleString('en-US',{maximumFractionDigits:0});
const token=v=>encodeURIComponent(String(v||'').trim()||'unknown');
function customerId(){try{const direct=String(localStorage.getItem('meshwar_customer_id')||'').trim();if(direct)return direct;const logged=JSON.parse(localStorage.getItem('loggedInUser')||'null');if(logged?.id)return String(logged.id).trim()}catch{}return'guest'}
function cartKey(){return `${PREFIX}_${token(customerId())}`}
let key=cartKey(),state=read(key),checkoutBusy=false;
function valid(x){return x&&String(x.store_id||'').trim()&&String(x.product_id||'').trim()&&Number(x.quantity)>0}
function normalize(input){const q=Math.max(1,Math.floor(Number(input.quantity)||1)),unit=Math.max(0,Number(input.unit_price_local)||0),old=Math.max(0,Number(input.old_unit_price_local)||0);return{store_id:String(input.store_id||'').trim(),store_name:String(input.store_name||'').trim(),product_id:String(input.product_id||'').trim(),product_name:String(input.product_name||'').trim(),image_url:String(input.image_url||''),selected_options:{color:String(input.selected_options?.color||''),size:String(input.selected_options?.size||''),volume:String(input.selected_options?.volume||'')},quantity:q,selected:input.selected!==false,unit_price_local:unit,old_unit_price_local:old||null,currency:String(input.currency||'IQD').trim().toUpperCase()||'IQD',pricing_snapshot:{vendor_price_usd:Number(input.pricing_snapshot?.vendor_price_usd)||0,customer_price_usd:Number(input.pricing_snapshot?.customer_price_usd)||0,exchange_rate:Number(input.pricing_snapshot?.exchange_rate)||0,commission_rate:Number(input.pricing_snapshot?.commission_rate)||0,captured_at:input.pricing_snapshot?.captured_at||new Date().toISOString()}}}
function read(k=key){try{const x=JSON.parse(localStorage.getItem(k)||'[]');return Array.isArray(x)?x.filter(valid).map(normalize):[]}catch{return[]}}
function syncCustomer(){const next=cartKey();if(next===key)return false;key=next;state=read(key);return true}
function optionsKey(o={}){return['color','size','volume'].map(k=>String(o[k]||'').trim()).join('|')}
function itemKey(x){return `${String(x.store_id)}::${String(x.product_id)}::${optionsKey(x.selected_options)}`}
function snapshot(){return state.map(x=>JSON.parse(JSON.stringify(x)))}
function count(){return state.reduce((n,x)=>n+Math.max(1,Number(x.quantity)||1),0)}
function selectedItems(){return state.filter(x=>x.selected!==false)}
function subtotal(items){return items.filter(x=>x.selected!==false).reduce((n,x)=>n+(Number(x.unit_price_local)||0)*(Number(x.quantity)||1),0)}
function group(items=state){return items.reduce((m,x)=>{const id=String(x.store_id);(m[id]??=[]).push(x);return m},{})}
function save(){try{localStorage.setItem(key,JSON.stringify(state))}catch(e){console.warn('KINTO global cart storage failed',e)}render();window.dispatchEvent(new CustomEvent('kinto:global-cart-change',{detail:{customerId:customerId(),items:snapshot()}}))}
function add(input){syncCustomer();const next=normalize({...input,selected:true});if(!valid(next))throw new Error('بيانات المنتج غير مكتملة.');const k=itemKey(next),found=state.find(x=>itemKey(x)===k);if(found){found.quantity=Math.max(1,Number(found.quantity)||1)+next.quantity;found.selected=true;found.unit_price_local=next.unit_price_local;found.old_unit_price_local=next.old_unit_price_local;found.pricing_snapshot=next.pricing_snapshot;found.product_name=next.product_name;found.image_url=next.image_url;found.store_name=next.store_name}else state.push(next);save();open();return snapshot()}
function setQuantity(k,q){syncCustomer();const item=state.find(x=>itemKey(x)===k);if(!item)return;item.quantity=Math.max(1,Math.floor(Number(q)||1));save()}
function setSelected(k,value){syncCustomer();const item=state.find(x=>itemKey(x)===k);if(!item)return;item.selected=value!==false;save()}
function remove(k){syncCustomer();state=state.filter(x=>itemKey(x)!==k);save()}
function clear(){syncCustomer();state=[];save()}
function open(){syncCustomer();render();document.documentElement.classList.add('kinto-local-cart-open')}
function close(){document.documentElement.classList.remove('kinto-local-cart-open')}
async function rest(path,{method='GET',body=null,timeout=9000,returnRepresentation=false}={}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeout);try{const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{method,signal:controller.signal,cache:'no-store',headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,Accept:'application/json','Content-Type':'application/json',...(['POST','PATCH'].includes(method)?{Prefer:returnRepresentation?'return=representation':'return=minimal'}:{})},body:body===null?null:JSON.stringify(body)});if(!r.ok)throw new Error((await r.text().catch(()=>''))||`HTTP ${r.status}`);if(r.status===204)return null;const text=await r.text();return text?JSON.parse(text):null}catch(e){if(e?.name==='AbortError')throw new Error('انتهت مهلة الاتصال أثناء إرسال الطلب.');throw e}finally{clearTimeout(timer)}}
async function loadCustomer(id){if(id==='guest')return null;const rows=await rest(`customers?select=id,name,phone&id=eq.${encodeURIComponent(id)}&limit=1`);const customer=Array.isArray(rows)?rows[0]:null;if(!customer)throw new Error('تعذر العثور على بيانات العميل الحالي.');return customer}
function storeSummaries(items){return Object.values(group(items)).map(list=>{const first=list[0]||{},qty=list.reduce((n,x)=>n+Math.max(1,Number(x.quantity)||1),0),sum=list.reduce((n,x)=>n+(Number(x.unit_price_local)||0)*(Number(x.quantity)||1),0);return{store_id:String(first.store_id||''),store_name:String(first.store_name||'متجر محلي'),item_count:list.length,quantity:qty,subtotal_local:sum,currency:first.currency||'IQD',status:'بانتظار التسديد',status_updated_at:null}})}
function removeSubmitted(keys){const sent=new Set(keys);state=state.filter(x=>!sent.has(itemKey(x)));save()}
async function checkout(){syncCustomer();if(checkoutBusy)return;const items=selectedItems();if(!items.length)return alert('حدد منتجاً واحداً على الأقل لإتمام الطلب.');const total=subtotal(items);if(!(total>0))return alert('إجمالي المنتجات المحددة غير صالح.');checkoutBusy=true;render();try{const customer=await loadCustomer(customerId()),totalQty=items.reduce((n,x)=>n+Math.max(1,Number(x.quantity)||1),0),submittedKeys=items.map(itemKey),bundleItems=items.map(x=>({store_id:String(x.store_id),store_name:x.store_name||'متجر محلي',product_id:String(x.product_id),product_name:x.product_name||'',product_image:x.image_url||null,selected_options:{...x.selected_options},quantity:Math.max(1,Number(x.quantity)||1),unit_price_local:Number(x.unit_price_local)||0,line_total_local:(Number(x.unit_price_local)||0)*(Number(x.quantity)||1),currency:x.currency||'IQD',pricing_snapshot:{...x.pricing_snapshot}})),stores=storeSummaries(items),details={source:'local_cart_bundle',bundle_version:'v94-global-multistore',multi_store:true,customer_scope_id:customerId(),customer_scope_type:customerId()==='guest'?'guest':'customer',items:bundleItems,stores,item_count:items.length,store_count:stores.length,quantity:totalQty,requested_quantity:totalQty,goods_total_local:total,customer_total_local:total,manual_fees_pending:true,local_currency:'IQD',submitted_at:new Date().toISOString(),bundle_locked:true};const payload={customer_name:customer?.name||'ضيف',customer_phone:customer?.phone||'',total_price:total,currency:'IQD',details,order_url:'customer-dashboard.html',image_url:items[0]?.image_url||null,status:'انتظار رد الموظف'};if(customerId()!=='guest')payload.customer_id=customer.id;const created=await rest('orders',{method:'POST',body:[payload],returnRepresentation:true});if(!Array.isArray(created)||!created.length)throw new Error('لم يؤكد النظام إنشاء الطلب.');const canonicalCode=String(created[0]?.order_code||'').trim();if(!/^KN-\d{6,}$/.test(canonicalCode))throw new Error('لم تُرجع قاعدة البيانات رقم KN متسلسلاً صالحاً.');removeSubmitted(submittedKeys);const success=document.getElementById('kintoLocalCartSuccess');if(success){success.textContent=`تم إرسال الطلب العالمي بنجاح — رقم الطلب: ${canonicalCode}`;success.hidden=false}render();open();return{ok:true,orderCode:canonicalCode,order:created[0]}}catch(e){console.error('KINTO V94 global checkout failed',e);alert('تعذر إرسال الطلب: '+(e.message||e));return{ok:false,error:e}}finally{checkoutBusy=false;render()}}
function ensureUi(){if(document.getElementById('kintoLocalCartDrawer'))return;const trigger=document.createElement('button');trigger.type='button';trigger.className='kinto-local-cart-trigger';trigger.id='kintoLocalCartTrigger';trigger.innerHTML='🛒 السلة الموحدة <span class="kinto-local-cart-count" id="kintoLocalCartCount">0</span>';trigger.addEventListener('click',open);document.body.appendChild(trigger);const back=document.createElement('div');back.className='kinto-local-cart-backdrop';back.addEventListener('click',close);document.body.appendChild(back);const drawer=document.createElement('aside');drawer.id='kintoLocalCartDrawer';drawer.className='kinto-local-cart-drawer';drawer.innerHTML='<div class="kinto-local-cart-head"><h2 class="kinto-local-cart-title">السلة الموحدة — جميع المتاجر</h2><button type="button" class="kinto-local-cart-close">×</button></div><div class="kinto-local-cart-body" id="kintoLocalCartBody"></div><div class="kinto-local-cart-foot"><div id="kintoLocalCartSuccess" class="kinto-local-cart-success" hidden></div><div class="kinto-local-cart-grand"><span>المجموع العالمي</span><strong id="kintoLocalCartGrand">IQD 0</strong></div><button type="button" class="kinto-local-cart-checkout" id="kintoLocalCartCheckout">إتمام وإرسال طلب واحد</button><div class="kinto-local-cart-note">رسوم الشحن والتوصيل لا توزع على المتاجر، ويضيفها الموظف يدوياً عند الحاجة.</div></div>';drawer.querySelector('.kinto-local-cart-close').addEventListener('click',close);drawer.querySelector('#kintoLocalCartCheckout').addEventListener('click',checkout);drawer.addEventListener('change',e=>{const box=e.target.closest?.('[data-cart-select]');if(box)setSelected(box.dataset.key,box.checked)});drawer.addEventListener('click',e=>{const b=e.target.closest('[data-cart-action]');if(!b)return;const k=b.dataset.key,item=state.find(x=>itemKey(x)===k);if(b.dataset.cartAction==='remove')remove(k);else if(item&&b.dataset.cartAction==='plus')setQuantity(k,item.quantity+1);else if(item&&b.dataset.cartAction==='minus')setQuantity(k,Math.max(1,item.quantity-1))});document.body.appendChild(drawer)}
function render(){syncCustomer();ensureUi();const countEl=document.getElementById('kintoLocalCartCount'),body=document.getElementById('kintoLocalCartBody'),grand=document.getElementById('kintoLocalCartGrand'),checkoutBtn=document.getElementById('kintoLocalCartCheckout');if(countEl)countEl.textContent=String(count());if(!body||!grand)return;const selected=selectedItems();if(checkoutBtn){checkoutBtn.disabled=checkoutBusy||!selected.length;checkoutBtn.textContent=checkoutBusy?'جاري إرسال الطلب...':'إتمام وإرسال طلب واحد'}if(!state.length){body.innerHTML='<div class="kinto-local-cart-empty">السلة الموحدة فارغة حالياً.</div>';grand.textContent='IQD 0';return}body.innerHTML=Object.entries(group()).map(([store,items])=>{const currency=items[0]?.currency||'IQD';return `<section class="kinto-local-cart-store" data-store-id="${esc(store)}"><div class="kinto-local-cart-store-title">🏪 ${esc(items[0]?.store_name||'متجر محلي')}</div>${items.map(x=>{const k=itemKey(x),opts=[x.selected_options?.color&&'اللون: '+x.selected_options.color,x.selected_options?.size&&'المقاس: '+x.selected_options.size,x.selected_options?.volume&&'الحجم: '+x.selected_options.volume].filter(Boolean).join(' · '),isSelected=x.selected!==false,line=(Number(x.unit_price_local)||0)*(Number(x.quantity)||1);return `<div class="kinto-local-cart-item ${isSelected?'':'is-unselected'}"><label class="kinto-local-cart-select" title="احتساب المنتج ضمن المجموع"><input type="checkbox" data-cart-select data-key="${esc(k)}" ${isSelected?'checked':''}><span aria-hidden="true"></span></label><img class="kinto-local-cart-img" src="${esc(x.image_url)}" alt=""><div><div class="kinto-local-cart-name">${esc(x.product_name)}</div>${opts?`<div class="kinto-local-cart-options">${esc(opts)}</div>`:''}<div class="kinto-local-cart-price"><span class="kinto-money" dir="ltr">${money(x.unit_price_local)} ${esc(currency)}</span><span class="kinto-price-op">×</span><span dir="ltr">${x.quantity}</span><span class="kinto-price-op">=</span><strong class="kinto-money" dir="ltr">${money(line)} ${esc(currency)}</strong></div><div class="kinto-local-cart-actions"><div class="kinto-local-cart-qty"><button data-cart-action="minus" data-key="${esc(k)}">−</button><span>${x.quantity}</span><button data-cart-action="plus" data-key="${esc(k)}">+</button></div><button class="kinto-local-cart-remove" data-cart-action="remove" data-key="${esc(k)}">حذف</button></div></div></div>`}).join('')}<div class="kinto-local-cart-store-total">مجموع المتجر: <span dir="ltr">${money(subtotal(items))} ${esc(currency)}</span></div></section>`}).join('');grand.innerHTML=`<span dir="ltr">${money(subtotal(state))} IQD</span>`}
const api={add,addItem:add,setQuantity,setSelected,remove,clear,checkout,getItems:()=>{syncCustomer();return snapshot()},getCount:()=>{syncCustomer();return count()},getScope:()=>({customerId:customerId(),key}),open,close};Object.defineProperty(api,'storageKey',{enumerable:true,get:()=>key});window.KintoGlobalCartV94=api;window.KintoLocalCartV93=api;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',render,{once:true});else render();window.addEventListener('storage',e=>{if(e.key===key){state=read();render();return}if(['meshwar_customer_id','loggedInUser'].includes(e.key)){syncCustomer();render()}});
})();
