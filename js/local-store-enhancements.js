import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';
const sb=createClient('https://hsmmbloouskqdnptiiad.supabase.co','sb_publishable_6_IDhNRdtxboDuCfBeAulQ_RRrBqpFH');
let currentStoreId='',currentStore=null,currentProducts=[];
const style=document.createElement('style');style.textContent=`#localStoreProductsGrid .local-public-card{overflow:hidden!important;padding:0!important;text-align:right!important;min-height:0!important}#localStoreProductsGrid .local-public-logo{display:block!important;width:100%!important;height:220px!important;max-width:none!important;object-fit:cover!important;border-radius:16px 16px 0 0!important;margin:0!important}#localStoreProductsGrid .local-public-name,#localStoreProductsGrid .local-public-meta,#localStoreProductsGrid .local-public-chip,#localStoreProductsGrid .local-public-card>div[style]{margin-left:14px!important;margin-right:14px!important}#localStoreProductsGrid .local-public-name{font-size:16px!important;margin-top:12px!important}.local-product-final-price{margin:10px 14px 0;padding:9px 10px;border-radius:10px;background:rgba(14,165,233,.12);border:1px solid rgba(56,189,248,.25);color:#67e8f9;font-size:15px;font-weight:900}.local-product-final-price small{display:block;margin-top:3px;color:#94a3b8;font-size:10px;font-weight:700}.local-product-order-btn{display:flex;width:calc(100% - 28px);margin:12px 14px 14px;padding:11px;align-items:center;justify-content:center;border-radius:12px;border:1px solid rgba(251,191,36,.55);background:linear-gradient(90deg,#0284c7,#4f46e5);color:#fff;font-weight:900;cursor:pointer}.local-product-order-btn:disabled{opacity:.45;cursor:not-allowed}@media(max-width:640px){#localStoreProductsGrid .local-public-logo{height:180px!important}}`;document.head.appendChild(style);
function vendorPrice(product){const n=Number(product?.discount_price??product?.base_price);return Number.isFinite(n)&&n>=0?n:null}
function commissionRate(store=currentStore){const n=Number(store?.commission_rate??10);return Number.isFinite(n)&&n>=0&&n<100?n:10}
function customerPrice(product,store=currentStore){const base=vendorPrice(product);if(base===null)return null;const rate=commissionRate(store)/100;return Math.ceil(base/(1-rate))}
function orderCurrency(product,store=currentStore){return String(product?.currency||store?.default_currency||'$').trim()||'$'}
async function loadStoreContext(id){const storeId=String(id||'').trim();if(!storeId)return;const[{data:store,error:se},{data:products,error:pe}]=await Promise.all([sb.from('local_stores').select('id,store_name,commission_rate,default_currency,status').eq('id',storeId).single(),sb.from('local_products').select('id,product_name,image_url,base_price,discount_price,currency,is_out_of_stock').eq('store_id',storeId).order('created_at',{ascending:false})]);if(se)throw se;if(pe)throw pe;currentStore=store||null;currentProducts=products||[]}
const oldOpen=window.openStore;window.openStore=async function(url,id){currentStoreId=String(id||'').trim();if(currentStoreId){try{await loadStoreContext(currentStoreId)}catch(e){console.error('Local store pricing context error:',e);currentStore=null;currentProducts=[]}}return oldOpen(url,id)};
async function placeOrder(product){const customerId=String(localStorage.getItem('meshwar_customer_id')||localStorage.getItem('viewingCustomerId')||'').trim();if(!customerId){alert('الرجاء تسجيل الدخول أولاً لإرسال الطلب.');location.href='login.html';return}try{if(!currentStore||String(currentStore.id)!==String(currentStoreId))await loadStoreContext(currentStoreId);const[{data:customer,error:ce},{data:rows,error:oe}]=await Promise.all([sb.from('customers').select('id,name,phone').eq('id',customerId).single(),sb.from('orders').select('order_code').not('order_code','is',null).order('created_at',{ascending:false}).limit(1000)]);if(ce)throw ce;if(oe)throw oe;const basePrice=vendorPrice(product),finalPrice=customerPrice(product,currentStore),rate=commissionRate(currentStore),currency=orderCurrency(product,currentStore);if(basePrice===null||finalPrice===null)throw new Error('سعر المنتج غير صالح.');let max=1000;(rows||[]).forEach(r=>{const m=String(r.order_code||'').match(/^MW-(\d+)$/i);if(m)max=Math.max(max,Number(m[1]))});const orderCode='MW-'+(max+1),platformMargin=finalPrice-basePrice;const payload={order_code:orderCode,customer_id:customer.id,customer_name:customer.name||'',customer_phone:customer.phone||'',total_price:finalPrice,currency,details:{source:'local_store',store_id:currentStoreId,store_name:currentStore?.store_name||'',product_id:String(product.id),product_name:product.product_name||'',vendor_price:basePrice,commission_rate:rate,platform_margin:platformMargin,customer_price:finalPrice,currency,quantity:1},order_url:'index.html?storeId='+encodeURIComponent(currentStoreId),image_url:product.image_url||null,status:'انتظار رد الموظف'};const{error}=await sb.from('orders').insert([payload]);if(error)throw error;alert('تم إرسال الطلب إلى إدارة الطلبات بنجاح. السعر: '+finalPrice+' '+currency+' — رقم الطلب: '+orderCode);location.href='dashboard.html?customerId='+encodeURIComponent(customer.id)}catch(e){console.error(e);alert('تعذر إرسال الطلب: '+(e.message||e))}}
function enhance(){const grid=document.getElementById('localStoreProductsGrid');if(!grid||!currentStoreId)return;[...grid.querySelectorAll('.local-public-card')].forEach((card,i)=>{const p=currentProducts[i];if(!p)return;const finalPrice=customerPrice(p,currentStore),currency=orderCurrency(p,currentStore);let priceBox=card.querySelector('.local-product-final-price');if(!priceBox){priceBox=document.createElement('div');priceBox.className='local-product-final-price';const chip=card.querySelector('.local-public-chip');if(chip)chip.style.display='none';card.appendChild(priceBox)}priceBox.innerHTML=p.is_out_of_stock?'غير متوفر حالياً':`${finalPrice??'---'} ${currency}<small>السعر شامل هامش MeshWar — التوصيل يحدد لاحقاً</small>`;let b=card.querySelector('.local-product-order-btn');if(!b){b=document.createElement('button');b.type='button';b.className='local-product-order-btn';card.appendChild(b)}b.disabled=!!p.is_out_of_stock||finalPrice===null;b.textContent=p.is_out_of_stock?'غير متوفر حالياً':'🛒 اطلب الآن';b.onclick=()=>placeOrder(p)})}
new MutationObserver(enhance).observe(document.getElementById('localStoreProductsGrid'),{childList:true,subtree:true});
const q=new URLSearchParams(location.search).get('storeId');if(q){currentStoreId=q;try{await loadStoreContext(q)}catch(e){console.error('Initial local store pricing load error:',e)}setTimeout(enhance,700)}
