/* KINTO V17 — visible brand purge + featured store transformation */
(()=>{'use strict';
const BRAND_RE=/منصة\s*MeshWar|MeshWar/gi;
function replaceVisibleBrand(root=document.body){if(!root)return;const walker=document.createTreeWalker(root,NodeFilter.SHOW_TEXT,{acceptNode(n){const p=n.parentElement;if(!p||/^(SCRIPT|STYLE|NOSCRIPT|TEMPLATE)$/i.test(p.tagName))return NodeFilter.FILTER_REJECT;return BRAND_RE.test(n.nodeValue||'')?NodeFilter.FILTER_ACCEPT:NodeFilter.FILTER_REJECT}});const nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);nodes.forEach(n=>{BRAND_RE.lastIndex=0;n.nodeValue=(n.nodeValue||'').replace(BRAND_RE,m=>/^منصة/i.test(m)?'منصة KINTO':'KINTO')});document.querySelectorAll('[alt],[title],[aria-label],[placeholder]').forEach(el=>['alt','title','aria-label','placeholder'].forEach(a=>{const v=el.getAttribute(a);if(v&&/MeshWar/i.test(v))el.setAttribute(a,v.replace(/MeshWar/gi,'KINTO'))}));if(/MeshWar/i.test(document.title))document.title=document.title.replace(/MeshWar/gi,'KINTO')}
function featuredStore(){const box=document.querySelector('.header-container');if(!box||box.classList.contains('kinto-featured-store'))return;const logo=box.querySelector('.main-logo')||document.createElement('img');logo.className='main-logo';logo.alt='KINTO Logo';const wrap=document.createElement('div');wrap.className='kinto-featured-logo-wrap';wrap.appendChild(logo);const copy=document.createElement('div');copy.className='kinto-featured-copy';copy.innerHTML='<span class="kinto-featured-badge">✦ متجر مختار</span><h1 class="kinto-featured-title">KINTO <span class="kinto-featured-accent">Featured Store</span></h1><p class="kinto-featured-subtitle">واجهة مختارة بعناية لتجربة تسوق وشحن متكاملة بهوية KINTO الفاخرة.</p>';box.replaceChildren(wrap,copy);box.classList.add('kinto-featured-store')}
function enforceNav(){const nav=document.querySelector('body>nav');if(!nav)return;nav.style.setProperty('background','#063b32','important');nav.style.setProperty('background-image','linear-gradient(135deg,#063b32 0%,#08483c 55%,#032b25 100%)','important');const brand=nav.querySelector('a[href="index.html"]');brand?.querySelectorAll('span').forEach(x=>x.remove())}
function apply(){featuredStore();replaceVisibleBrand();enforceNav()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',apply,{once:true});else apply();
setTimeout(apply,300);setTimeout(apply,1200);
const mo=new MutationObserver(()=>{clearTimeout(window.__kintoV17Tick);window.__kintoV17Tick=setTimeout(()=>{replaceVisibleBrand();enforceNav()},40)});if(document.body)mo.observe(document.body,{childList:true,subtree:true,characterData:true});else document.addEventListener('DOMContentLoaded',()=>mo.observe(document.body,{childList:true,subtree:true,characterData:true}),{once:true});
})();
